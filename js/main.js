console.log("Addiction Uncovered: App Initialized");

// Basic mobile menu toggle if we add one later
document.addEventListener('DOMContentLoaded', () => {
    console.log("DOM fully loaded and parsed");
});
